# somegame 设计文档 v1.0

> 修订记录（2026-09-09，从"初期设计"定稿为 v1.0 规格）：
> 1. 撤销 P2P/无中心服方案，改为**托管服务器权威仲裁 + 房间化多局并发**；
> 2. 明确**胜负判定/终局结算模型**（事件触发式 + 全盘盘点，允许多人同胜）；
> 3. 明确**同身份 = 同阵营**、给出正式支持人数（4 / 8）配比与受保护 NPC 分配；
> 4. 受保护 NPC 拥有独立 HP，保镖"掩护"挡伤细则落地；
> 5. 统一**距离为曼哈顿距离、视野半径 2**，修正原"3x3 九宫格"自相矛盾；
> 6. 补充逐卡**使用时机语义**、陷阱统一结算、玩家死亡规则；
> 7. 卡池一期扩至 **90-120 张**，先经数值模拟校验再冻结；
> 8. 玩家间无直接私聊/语音，通讯只经游戏内"交谈"动作与行动记录。

---

## 1. 基本信息

本项目为基于 C# 后端 + Web UI 前端的类狼人杀 + 三国杀游戏，支持多人游玩，**支持多个房间同时进行多局游戏**。

- C#（.NET）使用当前支持的最新 LTS 版本，后端为**托管服务器（权威）**，客户端仅负责渲染与本地私人信息（手牌、己方情报）。
- 服务端架构：**生产者-消费者 + 纯数据驱动**的事件流水线。角色、卡牌、地图、配比、计时等全部以配置数据表达，避免硬编码，便于扩展。
- 通信采用 WebSocket（SignalR 亦可），服务器按每个客户端**应知信息最小化**下发。

术语约定（后文沿用）：

| 术语 | 含义 |
| --- | --- |
| 回合 | 大回合制，含"自由行动 → 身份查验 → 结算与回收填充"等阶段 |
| 行动点（AP） | 自由行动阶段执行动作的资源 |
| 角色 / 玩家 | 参与游戏的真人，持有身份牌 |
| 普通 NPC | 背景填充的非玩家角色，用于混淆身份 |
| 受保护 NPC | 本局保镖需保护、杀手需击杀、小偷需行窃的指定目标（有独立 HP） |
| 撤离点 | 地图上的核验点；NPC/玩家到达即触发相关结算 |
| 隐匿 | 隐身等效果带来的状态，用于隐藏行动或获得攻击判定加成 |

---

## 2. 身份、配比与目标

### 2.1 四种身份

| 身份 | 胜利目标（阵营级） | 默认 HP | 每回合固定补给 | 专属/固有能力 |
| --- | --- | --- | --- | --- |
| 保镖 | 守护**指定受保护 NPC 及其财物**：NPC 存活并抵达撤离点、其指定财物未被窃 | 5 | 无 | 固有指令"掩护" |
| 杀手 | 击杀**指定受保护 NPC**，随后本人存活并抵达撤离点核验 | 3 | 临时枪械攻击 ×1（虚无/消耗） | — |
| 小偷 | 窃取**指定 NPC 的指定财物**，随后本人携物存活抵达撤离点核验 | 2 | 临时隐身 ×1（虚无/消耗） | 固有动作"窃取" |
| 疯子 | 击杀本局所有保镖、杀手、小偷 | 4 | 临时刀械攻击 ×1（虚无/消耗） | — |

> 说明：
> - 杀手/小偷的"撤离核验"是**分步累积**：先达成动作条件（击杀 / 窃取），再于撤离点完成核验，二者不必同一时刻成立。
> - "虚无"= 临时生成卡，非实体卡牌，不出现在地图回收池中；"消耗"= 用后即弃，不进入回收池。
> - 保镖无回合固定补给；其强度来自 5 HP、掩护与卡池中的防御/治疗支持。

### 2.2 支持人数与配比（v1 只开放 4 / 8）

| 人数 | 身份牌 | 受保护 NPC | 配置 |
| --- | --- | --- | --- |
| 4（推荐） | 保镖×1、杀手×1、小偷×1、疯子×1 | 1 个（单案） | 单对抗三角 + 疯子清场 |
| 8（推荐） | 保镖×2、杀手×2、小偷×2、疯子×2 | 2 个（双案：案 A / 案 B） | 两套独立对抗线 + 疯子共享清场目标 |

**双案结构（8 人）**：两名受保护 NPC 记为 NPC-A、NPC-B，构成两个独立"案"：

- 案 A：保镖-A 保护 NPC-A（目标 A-财物）；杀手-A 击杀 NPC-A；小偷-A 窃取 A-财物。
- 案 B：保镖-B 保护 NPC-B（目标 B-财物）；杀手-B 击杀 NPC-B；小偷-B 窃取 B-财物。
- 两位疯子的共同目标：清除全部 6 名保镖/杀手/小偷，两者同阵营、共享胜利。

**阵营与共享胜利**：同身份 = 同阵营，共享胜利条件与胜利结算（疯子×2 共享清场；4 人局不存在同身份）。发牌时角色随机分配；受保护 NPC 与各角色的"指定"绑定在发牌时由服务器确定并写入玩家目标。

**v1 限定**：2/3/5/6/7 人默认禁用（UI 置灰）；其余人数可通过配置文件扩展（需另配目标分配），不在首期范围。

### 2.3 身份隐藏与情报

- 开局所有玩家与普通 NPC **身份不明**，仅知"有若干保镖/杀手/小偷/疯子在场（人数=配比）"。
- 受保护 NPC 有独一无二且可被观察的外在特征（可由高价值探查/交谈等发现其"重要人物"身份），但其**受保护属性与绑定案**仅对应保镖可见。
- 身份只能通过行动记录、行为模式、查验、NPC 指认等**推断 + 查验确认**。

---

## 3. 胜负判定与终局结算（事件触发式）

### 3.1 结算模型

终局采用**事件触发式 + 全盘盘点**：

1. 每个玩家的胜利 = 其"动作条件"（累计达成）＋（杀手/小偷的）"撤离核验"；
2. 任一下列**终局触发事件**发生，服务器立即做**全盘盘点**：将所有"条件已完全达成"的玩家/阵营记为胜者，允许多人乃至多方同胜；
3. 盘点后**游戏立即结束**，广播结果与全部身份。

### 3.2 终局触发事件（任一触发即结算）

| 事件 | 触发 | 常规胜者盘点（含同时达成的其他方） |
| --- | --- | --- |
| E1 NPC 安全撤离 | 任一受保护 NPC **存活**并从撤离点**成功撤离**（格内无敌对玩家则到达即撤离，否则原地等待满 3 回合），且其**指定财物未被窃取** | 对应保镖（及同阵营）胜 |
| E2 携物撤离 | 已窃取指定财物的小偷本人存活并抵达撤离点 | 该小偷（及其同阵营）胜；若多方已达成，一并计入 |
| E3 击杀后撤离 | 指定 NPC 已被击杀，且该案杀手本人存活并抵达撤离点 | 该杀手（及同阵营）胜 |
| E4 疯子清场 | 全部保镖/杀手/小偷均死亡 | 疯子（两人共享）胜 |
| E5 回合上限 | 到达配置的全局最大回合数仍未触发 E1-E4 | 无胜者（平局），或由配置改为"存活撤离点人数多者胜" |

**关键语义（修复早期自相矛盾）**：

- **NPC 死亡本身不终结游戏**。NPC 死亡瞬间会：① 作废该案保镖的胜利线；② 标记该案杀手的动作条件为"已达成"（此后其只需撤离核验即可触发 E3）。因为在击杀瞬间杀手必然尚未撤离，NPC 死亡不能作为终局判定点。
- E2/E3 都要求"本人存活且抵达撤离点"——若小偷/杀手在途中死亡或先被疯子清场，条件随之作废。
- 死亡玩家本人仍随阵营胜利共享（见 §3.3）。

### 3.3 玩家死亡规则

- 玩家死亡即出局，转为观战者（服务器放开全图视野，仅观战）。
- 死亡玩家的**未完成个人条件**作废（如死亡的小偷即便已携物也无法再核验；其财物掉落其所在格）。
- 若该玩家所属阵营仍有**存活成员**在之后达成阵营目标，死者随阵营**同胜**（共享胜利）。
- 死亡角色不再产生行动记录、不再被交谈/查验；NPC 对其记忆相关规则由服务器清理。
- 受保护 NPC 死亡则其案内一切胜负线清空（保镖失格；该案杀手仅剩"撤离核验"，但若其撤离即触发 E3——仍需本人活到撤离点）。

---

## 4. 回合流程

大回合制。未决出胜负则进入下一回合。受保护 NPC 每回合向撤离点移动，天然形成最大回合限制（见 §5.5）。

### 4.1 回合阶段总览

```
回合开始
 ├─ ① 情报结算与地图填充（每 N 回合）          —— 回收池重新随机填充 + NPC 例行行动广播
 ├─ ② 自由行动阶段（限时 + AP）                —— 无任何战斗行为；布置/侦查/交谈/移动
 ├─ ③ 身份查验阶段（按玩家编号顺序）            —— 可 查验 →（敌对且可战）→ 战斗（嵌套）
 └─ 回合结束判定：触发终局？未触发则 NPC 移动 → 下一回合
```

### 4.2 自由行动阶段

- 所有玩家**同时**行动，服务器按统一时钟与既定编号顺序逐条仲裁指令（模拟并行）。
- 每名玩家每回合 AP 上限：**默认 3**（配置），回合开始时回满，**不可结转**。
- 阶段结束条件（先到先止）：总时限到（默认 90s，配置）或所有玩家 AP 用尽。
- 禁止：一切造成伤害/击杀、攻击判定类行为（含枪械/刀械攻击）。
- 允许：移动、交谈、物品交互、高价值探查、情报/增益/治疗/补给/陷阱布置/窃取（见 §7）等。
- 超时/AFK：服务器托管，自动采用最保守动作（不消耗 AP、不暴露身份）。

### 4.3 身份查验阶段

- 按玩家编号顺序依次进行；每人**每回合一次**查验机会，且有独立思考时限（默认 15s，配置）。
- 内容：指定场上任意一名角色进行身份查验（基于你在本回合前积累的情报）。服务器判定真实身份，受"身份伪装/行动模仿/追踪探查"等卡牌效果修正（见 §9）。
- 查验揭示结果后：
  - 若对方为**敌对身份**且你持有能命中当前距离的攻击方式，你可选择发起战斗（进入临时战斗阶段，见 §10）；
  - 若对方非敌对，则不能主动发起战斗。
- **敌对关系表**（数据驱动，可按扩展配置）：

| 我的身份 | 敌对方 |
| --- | --- |
| 保镖 | 杀手（威胁 NPC）、小偷（窃财物）、疯子（清场） |
| 杀手 | 保镖（护 NPC 者）、疯子 |
| 小偷 | 保镖、疯子 |
| 疯子 | 保镖、杀手、小偷（即全部） |

- 查验阶段玩家整体亦可使用"身份伪装"等卡（按卡时机）。

### 4.4 战斗与回合流转

- 战斗为嵌套子阶段，在某一玩家的查验时机内发生、结束或中断后，继续下一个玩家的查验。
- 全部玩家查验完毕且未终局 → 回合结束：NPC 例行移动 → 按需地图再填充 → 下一回合。

---

## 5. 地图、视野与情报

### 5.1 地图

- 所有 NPC 与玩家位于**矩形格子盘**内；坐标记为 [X,Y]。
- 单格不限制同时存在的人数。
- 每格可含若干**可交互物品**（含可寻获技能卡的高价值物、医疗包、可作掩体的道具、撤离点标记等）。
- 地图大小与主题在**开局前选定或随机**（配置定义主题模板）；格子内容按主题规则**随机化填充**。

### 5.2 距离与视野（v1 修正）

- **距离 = 曼哈顿距离**（|ΔX| + |ΔY|）。
- **视野 = 以自身为中心、距离 ≤ 2 的所有格子**（共 13 格，形似半径 2 的菱形）。取代早期"3x3 九宫格"表述，并使其与情报档位一致。
- 距离 2 之外的事件对你**不可见、不推送**（服务器不下发，防作弊）。

### 5.3 情报三档衰减

| 距离 | 观测对象 | 你能看到什么 |
| --- | --- | --- |
| 0（同格） | 全部角色 | 详细行动：执行者、动作类别、交互对象（若有）。角色若**有意隐藏行动**，则仅得"做了某事"的模糊信息 |
| 1（上/下/左/右直邻） | — | 简单描述：明确"某角色执行了某类动作"，不知交互对象与细节 |
| 2（对角格与次直邻格） | — | 模糊描述：只知"有人做了什么动作"，不知是谁、针对谁；隐藏行动者不可察觉 |

> 与旧文本的映射：直邻 = 距离 1（粗略）、对角/次直邻 = 距离 2（更模糊）；"有意隐藏行动的隐匿动作"会让记录进一步降档或消失（见 §8）。

### 5.4 受保护 NPC 与普通 NPC

- 普通 NPC：数量由配置按人数/地图规模生成（默认 **14 个**，通常远多于玩家），每回合执行移动、物品交互、相互交谈并留下记录，用于混淆身份。
- 受保护 NPC：默认 **5 HP**。**开局距撤离点必须 >5 格**；移动策略见 §5.5（不会盲目冲向撤离点）。
- 受保护 NPC 不持卡、不主动战斗、不避战；其受击保护机制见 §10.4。
- 受保护 NPC 的**指定财物**默认随 NPC 停留处存放（NPC 随身）；NPC 死亡后财物留在其死亡格，可被后续窃取。

### 5.5 撤离点、移动策略与最大回合

- 地图默认**唯一全局撤离点**（配置可多）；受保护 NPC 与玩家共用。
- **撤离点位置保密**：第 1-4 回合对所有人隐藏；第 5 回合开放给保镖与受保护 NPC；此后若受保护 NPC 身份暴露（被查验确认）则对全体开放；否则第 10 回合无条件开放。
- **受保护 NPC 移动策略**：
  - 撤离点未对贵宾开放（第 1-4 回合）或贵宾身份**未暴露**且未被催促：只随机移动一格或原地不动，**不会自主冲向撤离点**；
  - 第 5 回合起，保镖与守护贵宾**交谈即催促移动**，被催促的贵宾本回合向撤离点最近距离移动 1 格；
  - 贵宾**身份暴露**且撤离点开放：每回合确定向撤离点最近距离移动 1 格，被催促时移动 2 格；
  - 遇烟雾/燃烧等危险格先避险。
- **撤离结算**：贵宾抵达撤离点时，若该格**没有敌对玩家**（杀手/小偷/疯子）则立即撤离（触发 E1）；否则必须**原地等待满 3 回合**才能撤离。
- 全局另设**最大回合数**兜底（默认 120 回合，配置），触发 E5。

### 5.6 卡牌布置与回收再填充

- 开局：每名玩家抽 5 张手牌；剩余实体卡随机填充进**木箱**（可交互物品），另有若干**初始即空**的木箱与医疗包/掩体；**同一格可放多个可交互物品**。
- 木箱被取空后**保留在地图上**（变为空箱，可继续作为陷阱目标），默认可交互物品不显示是否为空，仅"高价值探查"提示"可能有东西"。
- 使用过的**实体卡**进入系统回收池；每隔 **N 回合**（默认 3，配置）回合开始阶段按既定规则将回收池随机填充回地图（生成新的木箱）。
- 临时生成卡（虚无/消耗：杀手/小偷/疯子补给、弹药包、管制刀具产物）不入回收池。

---

## 6. 对象模型与观测

- **玩家**：持身份、HP、AP、手牌；其行动产生记录，可被交谈/查验/攻击。
- **角色命名**：服务器为每个角色（含普通 NPC）分配公开代号（与身份无关，如"旅人 7 号"）；一切 UI 与记录使用代号，避免界面泄露身份信息。
- **受保护 NPC**：见 §5.4。
- 所有角色的 HP 归零即死亡（普通 NPC 亦如此，死亡 NPC 移出棋盘不再产生记录）。

---

## 7. 固有行动（与技能卡无关）

| 固有行动 | AP | 次数 | 说明 |
| --- | --- | --- | --- |
| 移动 | 1 | 每回合限 1 次 | 至多移动 3 格（曼哈顿距离累计 ≤3） |
| 高价值探查 | 0 | 每回合限 1 次 | 指出**当前格**哪些可交互物品"可能有东西"；无法分辨是物品还是陷阱，其余物品不显示是否为空 |
| 物品交互 | 1 | 不限 | 交互当前格指定可交互物品，获取其中隐藏物品；若持有适用技能卡，可在此对其使用（通常为布置陷阱） |
| 主动交谈 | 1 | 不限 | 与当前格一名角色交谈（见 §7.1 与 §8 的可疑/指认） |
| 掩护（保镖固有） | 0 | 每回合限 1 次 | 保镖与其守护 NPC 同格时可用；见 §10.4 |
| 窃取（小偷固有） | 1 | 每回合限 1 次 | 与目标 NPC 同格时执行；见 §7.2 |

### 7.1 主动交谈 / 可疑与指认

- 交谈对象无论玩家或 NPC，默认只在记录中留下符合其表面身份的问候。
- 若某玩家**连续执行相同操作**或不避嫌地**反复布置陷阱**，会使 NPC 产生"可疑"标记；之后其他玩家与这些 NPC 交谈时，会得到"指定角色一直在做 X，很可疑"的情报（用于收窄怀疑圈）。
- 若某玩家在早前查验中被判定敌对或存在**交战记录**，NPC 会"记住"；当其他玩家与这些 NPC 交谈且**该已暴露玩家仍在同格**时，NPC 会进行指认。
- 普通 NPC 也会自行移动、物品交互、相互交谈并留下记录。

### 7.2 窃取（小偷）

- 要求：小偷与目标受保护 NPC（或已死亡 NPC 的财物所在格）同格。
- 成功后，该案指定财物转移至小偷（其动作条件达成）；记录按 §8 暴露规则处理。
- 窃取属于"显性敌对行为"：在距离 0 记录中若未处于隐匿状态，将直接显示"窃取"，等同身份暴露（暴露类型）。隐匿（隐身）状态下执行则可隐藏该记录。

---

## 8. 行动记录与情报流

行动记录是玩家推断身份的**直接线索**；服务器在事件发生时即按各观看者的视野/距离**过滤后下发**，客户端不做全量推理。

### 8.1 距离档位记录模板

| 档位 | 记录样例 |
| --- | --- |
| 距离 0 | `[X,Y]「旅人7号」向东移动`；`[X,Y]「旅人7号」和「向导3号」交谈`；`[X,Y]「旅人7号」似乎从「木箱」发现了什么`；`[X,Y]「旅人7号」摆弄了一会「木箱」` |
| 距离 1 | `[X,Y]「旅人7号」移动了`；`[X,Y]「旅人7号」在和谁说话`；`[X,Y]「旅人7号」似乎在摆弄东西` |
| 距离 2 | `[X,Y] 有人移动了`；`[X,Y] 有人在说话`；`[X,Y] 有人在摆弄东西` |

- 距离 0 记录会明确指出动作类别与交互对象；**玩家针对物品交互/窃取是最易暴露"非 NPC"身份的关键线索**。
- 有意隐藏（隐匿）的动作：距离 0 降为"做了某事"的模糊信息；距离 1 仅有"有动作"；距离 2 不可察觉。
- UI 须为每条记录标注来源格与档位，并支持按格/按角色折叠、按关键词过滤。

---

## 9. 卡牌与技能体系

### 9.1 设计原则与卡池规模

- 玩家一切特殊行动与战斗行动需技能卡支持（三国杀式）。卡牌均以**数据表**表达（类型、可用阶段、目标、效果、数值、是否入回收池等）。
- 一期卡池规模：**90-120 张**。目标构成：攻击/防御类（枪械攻击、刀械攻击、闪避、防暴盾、兴奋剂等）约 **55-60%**；功能/情报类约 **25%**；身份对抗类（伪装/模仿/追踪探查等）约 **15%**；核心战斗类可超过单类 2-4 张的约束，其余特殊类每种 2-4 张。
- **平衡校验前置**：先以数值模拟/模拟器验证"两名玩家在含临时补给（弹药包/管制刀具/每回合固定补给）的条件下交战时，平均能形成约 3 次攻防对抗"，命中目标后再冻结数量分布。若不足，优先扩至 120 张或上调弹药类卡张数。
- **首期冻结配比（2026-09-10 模拟校验通过）**：一期 102 张，攻防类 59 张（57.8%，目标 55-60%），特殊类每型 2-4 张；`tools/SomeGame.Balance` 生成的《卡池与对局数值报告》见 `docs/balance-report.md`。模拟结论：裸手牌枪战约 1.6 次对抗偏低，计入杀手补给/弹药包等临时卡后为 3.6-5.6 次，符合"算上临时卡平均三次对抗"的预期；单场战斗以消耗战为主，胜负主要靠跨回合磨损与撤离线结算。

### 9.2 卡牌类型清单与默认时机

> 时机列：**自由** = 自由行动阶段可用；**查验** = 身份查验阶段可用；**战斗** = 临时战斗阶段可用；**任时** = 上述皆可。造成伤害/攻击判定类效果仅存在于"战斗"卡。以下为默认语义，全部参数与文案入配置表。

| 卡牌 | 时机 | 目标 | 默认效果与语义 |
| --- | --- | --- | --- |
| 隐身 | 任时（战斗外为主） | 自身 | 进入"隐匿"状态以执行下一个动作；该动作若为可隐匿动作则其记录降档/消失；攻击判定类结算完成后解除；受到非隐匿攻击命中提前解除。注：隐匿在"发动枪械攻击结算时"仍生效（用于满足枪击 2 点命中条件），结算后再解除 |
| 窃听器 | 自由/查验 | 当前格 | 在所在格留下持续 3 回合的窃听器；玩家离开后仍可收到该格等同距离 1 的行动信息 |
| 治疗包 | 任时 | 一名角色 | 治疗 1 点 HP；**可对受保护 NPC 使用** |
| 瞄准狙击 | 任时 | 自身 | 下一张枪械攻击卡 100% 命中；受到攻击判定时该效果取消 |
| 烟雾弹 | 自由/战斗 | 自身当前格 | 非战斗：所在格被烟雾覆盖（无法获得情报），NPC 恐慌逃离；可作陷阱。战斗：使用后撤退并**强制中断**当前战斗 |
| 能量饮料 | 自由 | 自身 | 恢复 1 点 AP |
| 兴奋剂 | 任时 | 自身 | 获得枪械攻击防御（降低被枪械命中判定）；同时使你的刀械攻击伤害 +1 |
| 无人机探查 | 自由/查验 | 距离 ≤10 格内任意格 | 获得该格**本回合**等同距离 0 的行动信息（无人机最远探查距离为 10 格） |
| 弹药包 | 自由/查验 | 自身 | 生成两张临时枪械攻击（虚无/消耗） |
| 管制刀具 | 自由/查验 | 自身 | 生成两张临时刀械攻击（虚无/消耗） |
| 防暴盾 | 任时 | 自身 | 完全防御下一次枪械/刀械攻击 |
| 身份伪装 | 查验（回合前使用） | 自身 | 本回合内被身份查验指定时，返回当前格随机其他角色的身份；当前格无其他角色则无效；若你已暴露，使用后恢复"未暴露"状态 |
| 行动模仿 | 任时（回合前使用） | 当前格一名角色 | 本回合你在行动记录上的行为显示为该角色**上一回合**的行动，其余行动被掩盖 |
| 追踪探查 | 查验 | 一个地图格 | 若该格存在本回合执行了"伪装（已生效）或模仿"的玩家，将其暴露；用于反制身份伪装/行动模仿 |
| 染色球 | 自由/查验 | 当前格一名角色 | 高亮对象当前位置（仅对使用者显示），并在行动记录标注其移动轨迹；可作陷阱 |
| 燃烧瓶 | 自由 | 距离 ≤2 的一格 | 该格进入"燃烧"状态，NPC 恐慌逃离；可作陷阱 |
| 闪避 | 战斗 | 自身 | 受到其他玩家非隐匿攻击时回避此次攻击（**可应对刀械攻击**） |
| 枪械攻击 | 战斗 | 距离 ≤2 格内对象 | 1D6 命中判定：1 必败、6 必胜、2-5 浮动；基础 2 点即中（目标格仅一人、附近无可作掩体的可交互物、目标未察觉你、你处于隐匿），四条件每缺一提升 1 点命中需求，最坏需 6 |
| 快速连射 | 任时 | 自身 | 下一次枪械攻击进行 3 次命中判定 |
| 刀械攻击 | 战斗 | 距离 ≤1 格内对象 | 近身攻击；对方无防御应对时必然命中 |
| 万能胶 | 自由 | 一个可交互物品 | 布置陷阱：他人探查该物品时进入"粘住"状态，本回合无法再行动 |

**关于"可隐匿动作"**：移动、物品交互、窃取、放置窃听器/陷阱等被判定为可隐匿执行的动作；交谈、攻击等为显性动作。具体以技能数据表字段 `hideable` 为准。

### 9.3 陷阱统一结算

- 陷阱 = 布置于某**可交互物品**之上的状态（来源于万能胶/烟雾弹/染色球/燃烧瓶等）。
- 布置方式：对目标格的该物品执行"物品交互"，同时消耗适用技能卡；布置动作产生正常行动记录（可被隐匿隐藏）。
- 触发：任何角色（玩家或 NPC）此后**探查/交互该物品**时结算对应效果（粘住/烟雾/标记/点燃），一次性或持续回合数按卡定义。
- 反制：距离 0 的观察者可通过记录判断"有人对某物品布置过"，距离更远者只能看到模糊动作。

### 9.4 预留扩展位（二期，不在 v1 定义）

- 保镖/护送侧主动工具（如临时转移、撤离加速、护卫道具）。
- 反情报类（反窃听/干扰、伪造行动记录）。
- 应对被查验的泛用手段（当前仅伪装/模仿，条件苛刻）。
- 装备/成长维度（若一期刻意省略装备，需在此声明，避免与三国杀预期混淆）。

---

## 10. 战斗系统细则

### 10.1 进入条件

- 仅在"身份查验阶段"由发起方主动发起。
- 条件：① 查验对象被确认为**敌对身份**；② 双方处于**可交战范围**（曼哈顿距离 ≤2，且发起者持有能命中该距离的攻击方式：枪械射程 2、刀械射程 1）。
- 战斗在发起时刻的距离与格子上进行，**战斗中途不移动格**。

### 10.2 过程与暴露

- 双方以当前手牌（含临时卡）轮流出牌；发起方先手。
- 在战斗中使用攻击类动作即向对方暴露"你是敌对玩家"（不直接暴露具体身份类型）；防御卡（闪避/防暴盾）不构成暴露。
- 每步出牌有思考时限（默认 10s，配置）；超时托管为"不出牌/不动作"。

### 10.3 结束条件

- **决出胜负**：一方 HP 归零死亡，另一方胜（无人避战时必须打到底）。
- **手牌耗尽**：双方手牌均耗尽仍无死亡 → 战斗中止，双方各退至原位，回合继续（可于后续回合再战）。
- **主动中断**：任一方在战斗中使用烟雾弹 → 撤退并强制中断。
- 由疯子主导的战斗，击杀可计入其清场进度（E4）。

### 10.4 掩护与针对 NPC 的攻击

- 掩护：保镖与其守护的受保护 NPC **同格**时，每回合可免费执行一次"掩护"（保 镖 意图明确、不可反悔）。掩护效果为：本回合内针对该 NPC 的攻击伤害/攻击判定转移至保镖自身，并以保镖为对手进入（或转入）玩家间战斗。
- 针对 NPC 的直接攻击：仅能发生在该 NPC 所在格的玩家战斗语境中。若保镖在场且已掩护则无法绕过；若保镖**不在同格**或掩护次数已尽，则攻击命中 NPC，扣其 HP。
- NPC 不持卡、不避战、不可逃跑（除非状态效果驱散其所在格），其受击按 10.2-10.3 的战斗规则以攻击方出牌结算。
- 受保护 NPC HP 归零 → 标记死亡（触发 §3.3 / §3.2 相关线）。

### 10.5 伤害数值基准

- 枪械攻击 / 刀械攻击基础伤害 **1**；兴奋剂使你的刀械伤害 +1；治疗包 / 医疗包每次恢复 1。
- 数值全部入配置表，作为平衡调整入口。

---

## 11. 技术架构

### 11.1 部署与房间模型

- 单台**托管服务器**运行全部房间；客户端仅 Web UI。
- **房间化**：每个房间 = 一个独立游戏实例，Actor/Channel 隔离，事件流互不阻塞，支持多局并发。
- 房间生命周期：创建（建房者设地图大小/主题/人数配置）→ 大厅/准备 → 对局 → 结算 → 解散。
- 连接：WebSocket（或 SignalR）；断线可重连（服务端保留会话），超时托管。

### 11.2 权威与信任

- 服务器为**唯一权威**：持有全部隐藏状态（身份、手牌、受保护 NPC、地图物品、伪装等）并执行一切判定。
- 客户端**不可信**：服务器仅下发各客户端"应知信息"（视野/距离过滤后的记录、自己的手牌与私密情报）；战斗的骰子、命中、伤害全在服务器结算，防作弊与防信息泄露由下发边界保证。

### 11.3 仿真核心与数据驱动

- 核心为 **headless 确定性仿真**：输入为事件流（玩家指令、NPC AI 决策、计时器事件、随机数种子事件），经仲裁流水线（生产者-消费者）输出结果事件。
- 随机：对局使用统一种子 + 确定性 PRNG；发牌、地图填充、骰子皆由此产生，支持回放。
- **纯数据驱动**：角色/身份、配比、卡牌（效果参数）、地图主题/物品表、计时、回收周期、敌对关系表均为配置数据。**复杂技能不写成散落的 if 分支**，而统一抽象为"条件 + 效果组件（DSL/组件表）"，使新卡/新身份/新地图无需改引擎逻辑即可加入。

### 11.4 阶段状态机与时钟

- 回合阶段为**嵌套状态机**（自由 → 查验（可逐人嵌战斗）→ 结算填充 → 下一回合），以栈式推进；战斗是可中断的子状态机。
- 时钟以服务器为准；各阶段时限由配置驱动；超时/AFK 由服务器统一托管并广播。
- 全部事件记入**事件日志**（Event Sourcing），用于：回放、观战、断线恢复、复盘与数值模拟抽样。

### 11.5 前端职责

- 棋盘（菱形视野高亮、迷雾）、行动记录流（带来源格与档位标注、过滤/折叠）、目标选择（同格多人以代号中立列表呈现，防止界面泄密）、手牌与战斗界面、查验/撤离/倒计时面板。
- 前端只是状态机渲染器，不做任何判定。

---

## 12. 默认配置表（config，全部可覆盖）

| 键 | 默认值 | 说明 |
| --- | --- | --- |
| 支持人数 | 4 / 8 | 其余默认禁用 |
| 受保护 NPC HP | 5 | |
| 玩家 HP | 保镖 5 / 杀手 3 / 小偷 2 / 疯子 4 | |
| 每回合 AP | 3 | 回合初回满，不结转 |
| 自由阶段时限 | 90 s | |
| 查验思考时限 | 15 s | |
| 战斗出牌时限 | 10 s | |
| 地图填充回收周期 | 每 3 回合 | |
| 开局手牌 | 5 张 | |
| 地图 | 大小/主题开局选定或随机 | 主题模板含可交互物品表 |
| 最大回合数 | 120 | E5 兜底 |
| 枪/刀基础伤害 | 1 | 兴奋剂刀 +1 |
| 卡池规模 | 90-120 | 先模拟校验后冻结 |
| 窃听器持续 | 3 回合 | |

---

## 13. 待办 / 下一步

1. 用数值模拟器校验卡池分布（§9.1）与双案/多阵营平衡；
2. 将配置表落为 schema（身份/卡牌/地图/配比/计时 JSON）；
3. 实现 headless 仿真核心与回合/战斗状态机；
4. 设计并实现房间大厅、对局与观战/回放的 Web UI 原型。
